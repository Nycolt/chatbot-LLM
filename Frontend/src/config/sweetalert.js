/**
 * Configuración global de SweetAlert2
 * Establece valores por defecto para todos los modales
 */
export const sweetAlertDefaults = {
  background: '#1e293b',
  color: '#ffffff',
  confirmButtonColor: '#dc2626',
  cancelButtonColor: 'transparent',
};

export const initSweetAlert = () => {
  // No es necesario inicializar nada, solo exportar los defaults
  console.log('SweetAlert2 defaults configurados');
};
